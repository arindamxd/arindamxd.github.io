---
import { isAnalyticsEnabled } from "../utils/analytics";

/** Production-only GA4 + Microsoft Clarity (see `src/scripts/analytics.ts`). */
const enabled = isAnalyticsEnabled();
---

{enabled && <script src="../scripts/analytics.ts" />}
